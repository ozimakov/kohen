# v1.0 Delivery Plan

> Source: peer gap review
> [`v1.0-gap-review.md`](https://github.com/ozimakov/kohen/blob/claude/1-0-review-gaps-a5q922/docs/reviews/v1.0-gap-review.md)
> · Date: 2026-07-16 · Goal: credible `v1.0.0` tag + docs/site readiness

## Decisions (what ships in 1.0)

| Gap / workstream | 1.0 decision | Rationale |
| --- | --- | --- |
| **1.1** stamp survives delete | **Fix** — retract `kohen-stamp` in finalizer | Real leftover annotation; R-WIRE.6 / R11.3 |
| **1.2** stale `rolloutInProgress` | **Fix** — reset on `none` + wiring failures | Status honesty (R11.2) |
| **1.3** SSA wire every reconcile | **Fix** — skip `Wire` when pod-template stamp matches (`auto`); skip `StampNoRestart` when object stamp matches (`none`) | Matches A3 / R-ROLLOUT.2 / T3 |
| **2.1** poll vs watch rotation | **Amend SPEC** — rotation detection is poll-bounded (≤ `sync.interval`); watch-driven rotation is post-1.0 | Label-restricted Secret cache is deliberate (TM8/T6); honest for 1.0 |
| **2.2** simulated GitOps A10 | **Keep + document** — simulated SSA manager remains the CI gate; real Argo/Flux is post-1.0 nightly | Mechanically equivalent; protects CI latency |
| **2.3** retry jitter | **Defer** — rely on controller-runtime defaults; note in SPEC | Low user impact |
| **2.4** R-AUTH.6 CEL wording | **Amend SPEC** — reconcile-time label check (fail-closed) | CEL cannot do cross-object lookup |
| **2.5–2.8** auth redirects / mounts / manifests / commit fetch | **Document** shipped behavior in SPEC/security docs | Fail-closed where it matters; no silent security regression |
| **3.x** observability | **Fix** — events for resolution/apply/prune; secret success counter; wiring-reason counters; stuck rollout → degraded gauge | R13.1 / R13.4 |
| **4.x** release artifacts | **Harden** — hard-fail OCI push + cosign; ship namespaced bundle; bump chart/SECURITY to 1.0.0; full Apache text; CHANGELOG | S3.3 / NFR10 |
| **4.5 / WS-G** API `v1alpha1` | **Keep `v1alpha1`** with documented product SemVer vs API version | Conversion not expected in v1.0.x; zero users today but alpha API is honest |
| **5.x** docs/spec drift | **Truth-up** SPEC to v1.0; close §18 Q1; conditions table; README fields; K8s floor 1.28+; T6 footprint | NFR7 |
| **WS-D** real GitOps e2e | **Post-1.0** | |
| **Site** | **Ship** GitHub Pages site from `site/` (published to `gh-pages`) | User-facing 1.0 surface; enable Pages → branch `gh-pages` once |

## Suggested cut order

1. Correctness (1.1–1.3) + tests  
2. Observability (3.x)  
3. SPEC/docs/version truth-up (WS-E + versions)  
4. Release pipeline (WS-F) + site + CHANGELOG  
5. Publish GitHub Release `v1.0.0-rc.1` (triggers release.yml) → A12 from previous tag → Release `v1.0.0`

## Out of 1.0 (explicit)

- Metadata-only Secret/ExternalSecret watches (post-1.0, restores full §6.1 watch semantics)
- Real Argo CD / Flux A10 job
- API promote to `v1beta1` / `v1`
- Nested mount overlap rejection beyond exact equality
- Redirect re-check against source allow-list (document; harden post-1.0)
