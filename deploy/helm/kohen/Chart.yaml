apiVersion: v2
name: kohen
description: |-
  Kohen — a Kubernetes-native operator that keeps workloads in sync with
  configuration stored in a git repository (SPEC §7, §16).
type: application
# Chart version; bump on chart changes.
version: 1.0.0
# App version tracks the operator image tag by default.
appVersion: "1.0.0"
kubeVersion: ">=1.28.0-0"
home: https://github.com/ozimakov/kohen
sources:
  - https://github.com/ozimakov/kohen
keywords:
  - gitops
  - configuration
  - operator
